import 'package:flutter/material.dart';

// Paleta oficial do SIDMA — usada em todas as telas para manter consistência visual.
class AppColors {
  static const azulPrincipal = Color(0xFF0D6EFD);
  static const verdePrincipal = Color(0xFF74C319);
  static const fundo = Color(0xFFF8FAFC);
  static const textoPrimario = Color(0xFF1E293B);
}